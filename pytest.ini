[pytest]
# Run tests from the project root so imports (app, intake, procurement_config) resolve correctly
testpaths = tests
# Show short summary of passes, failures, and skips
addopts = -v --tb=short
asyncio_default_fixture_loop_scope = function
